%h2= t("categories.edit.title", :name => @category.title)
- form_for @category, :url => category_path(@category) do |f|
    
  = f.label :title
  = f.text_field :title

  = f.label :description
  = f.text_area :description

  = f.label :slug
  = f.text_field :slug

  .submit-wrapper
    = f.submit