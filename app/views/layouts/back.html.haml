!!!
%html
%head
  %title= t("app_name")
  = stylesheet_link_tag "compiled/back", "jquery.gritter"
  = javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.5.2/jquery.min.js", "jquery.gritter.min.js", "back.js" 

  = csrf_meta_tag 

%body
  - unless flash.blank?
    :javascript
      $(function(){
        startGritter("#{escape_javascript flash[:notice][:title]}", "#{escape_javascript flash[:notice][:text]}");
      });
    
  %header
    %h1= t("back.title")
  
  - if @current_portfolio.setup?
    = render :partial => "/shared/nav"
    
  #content
    = yield 

  %footer
    &nbsp;
  
  