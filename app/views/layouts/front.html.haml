!!!
%html
%head
  %title= @portfolio.title
  - if @portfolio.theme_id.blank?
    %style{:media => "screen", :rel => "stylesheet", :type => "text/css" }
      = @portfolio.css
  - else
    = stylesheet_link_tag "themes/#{@portfolio.theme[:code]}"
    
%body{:id => @category.blank? ? "" : "category-#{@category.id}"}
  %header
    %h1= @portfolio.title
    %p= @portfolio.description

  %nav
    - @portfolio.categories.each do |category|
      .nav-link-wrapper{:class => (!@category.nil? and @category.id == category.id ? "selected" : "")}
        = link_to category.title, showcase_path(:slug => @portfolio.slug, :category_slug => category.slug)
    .nav-link-wrapper.about{:class => (@category.nil? ? "selected" : "")}
      = link_to "About", about_path(:slug => @portfolio.slug)
      
  #content 
    = yield 

  %footer
    = @portfolio.footer
    
  / 
    If really necessary, removing credits can be done by contacting us directly.
    In the meantime, do not remove credits using CSS or face our wrath.
    Feel free to style it, but please be nice and keep it visible somehow.
    
  #footest
    %span.rlvnt 
      Portfolio powered by RLVNT
    - unless @portfolio.theme_id.blank?
      %span.theme
        Original theme by #{@portfolio.theme[:user]} under license Creative Commons